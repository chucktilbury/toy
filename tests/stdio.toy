; Implement the stdio library for toy

nothing print(string str) {

    ; string was formatted before this call.
    inline {
        fputs(TOY_str, stdout);
    }
}

integer read() {

    inline {
        // do something smart here
    }
}

nothing exit(int result) {

    inline {
        // call the system exit
        exit((int)TOY_result);
    }
}
