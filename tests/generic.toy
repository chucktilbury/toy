

# comments are good
; this file is just generic syntax tests to check the raw parser.

int ploot
string foobart

import "tests/something.toy"
; Making this a formatted string will be implemented after the symbol table
; is working.
; import "something.toy"()
; import "something.toy"(123, 123, 123)

nothing doodah(int snark, float zoop) {

    int asdf = snark + 123 / ploot
    int flarp = zoop
    int fart
}

; this is equvent to the a "main" function
start{

    int plart = asdf(qwer, oier(1, 2, 3))
    IF(not 1 == 2) {
        printf("[foo bar"(1, 2, 3))
    }
    else (123) { printf() }
    else {
        print("bar foo"(3, 4, 5), 123) ; this is a comment
    }

    inline {
        // This is another inline block
        for(int a = 0; a < 100; a++) {
            if(a == 123) {
                printf("foo the heck the bar\n");
            }
        }
        // and a comment
    }

   { as = ewr }
    asd = 123 + 234;

    if(plart) {
        printf("spoof")
        while {        yield(1)}
    }

    print("asdf\e[1;31m234234\e[0m"(123, 34))
}
