/*
 * This generates the cumulative type of an expression result, based on the
 * left and right operands and the operator. The cumulative type that then
 * be used to validate an assignement.
 */

int expression_binary_type(int left, int right) {
}

int expression_unary_type(int left, int right) {
}
